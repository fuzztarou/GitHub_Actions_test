name: Workflow test

on:
  push:
    branches:
      - main

jobs:
  test_job:
    runs-on: ubuntu-latest
    permissions:
      contents: "write"
      id-token: "write"
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2

      - name: add a new file
        run: echo touch "$(date +%Y-%m-%d-%H-%M-%S)".txt

      - name: Commit/Push
        run: |
          git config user.name actions-user
          git config user.email action@github.com
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -m "[test] file generated by GitHub Actions"
            git push origin HEAD:${GITHUB_REF}
          fi
